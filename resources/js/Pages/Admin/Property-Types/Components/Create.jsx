import React from "react";
import Form from "./Form";

const Create = ({ closeForm }) => {
  return (
    <div>
      <Form formData={null} closeForm={closeForm} />
    </div>
  );
};

export default Create;
